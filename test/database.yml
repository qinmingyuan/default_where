sqlite:
  adapter: sqlite3
  database: "file:memdb1?mode=memory&cache=shared"

mysql:
  adapter: mysql2
  username: root
  password:
  database: default_where

postgresql:
  adapter: postgresql
  username: postgres
  password:
  database: default_where
  min_messages: ERROR
